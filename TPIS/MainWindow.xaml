<Window x:Class="TPIS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TPIS"
        xmlns:s="clr-namespace:TPIS.TPISCanvas"
        mc:Ignorable="d"
        Title="TPIS" WindowStartupLocation="CenterScreen" ResizeMode="CanResize"
        MinHeight="600" MinWidth="800">
    <Window.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Views/Modules/TPISMenu.xaml"/>
                <ResourceDictionary Source="Views/Modules/TPISToolBar.xaml"/>
                <ResourceDictionary Source="Views/Modules/TPISResManagementBox.xaml"/>
                <ResourceDictionary Source="Views/Modules/TPISCanvas.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
        </ResourceDictionary>
    </Window.Resources>
    <DockPanel>
        <ContentControl DockPanel.Dock="Top" Name="Menu" Content="{StaticResource TPISMenu}"/>
        <ContentControl DockPanel.Dock="Top" Content="{StaticResource TPISToolBar}"/>
        <ContentControl DockPanel.Dock="Left" Content="{StaticResource TPISResManagementBox}"/>
        <ContentControl DockPanel.Dock="right" >
            <DockPanel>
                <!--元件模块-->
                <Border BorderThickness="1" BorderBrush="Gray">
                    <GroupBox Header="属性" Margin="3,0,0,0">
                        <ListView HorizontalAlignment="Left" MinWidth="300">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="属性" Width="100"></GridViewColumn>
                                    <GridViewColumn Header="值" Width="100"></GridViewColumn>
                                    <GridViewColumn Header="单位" Width="40"></GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </GroupBox>
                </Border>
            </DockPanel>
        </ContentControl>
        <Grid>
            <Border BorderThickness="1" BorderBrush="Black" Margin="2">
                <DockPanel>
                    <StatusBar DockPanel.Dock="Bottom" Background="SkyBlue" MaxHeight="30">
                        <StatusBarItem Name="statusBar" Content="状态栏" HorizontalAlignment="Right" Focusable="False"/>
                    </StatusBar>

                    <TabControl x:Name="projectTab" Background="#FF7E7A7A" SelectionChanged="ProjectTab_SelectionChanged" >
                        <TabControl.ItemTemplate>
                            <DataTemplate>
                                <DockPanel Width="90" MinWidth="30" Background="AliceBlue">
                                    <Button Tag="{Binding Name}" DockPanel.Dock="Right" Click="ProjectItem_Close" >
                                        <Image Source="Images/icon/tab_close.png" Width="10" Height="10"/>
                                    </Button>
                                    <TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"></TextBlock>
                                </DockPanel>
                            </DataTemplate>
                        </TabControl.ItemTemplate>
                        <TabControl.ContentTemplate>
                            <DataTemplate>
                                <Grid>
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" VerticalAlignment="Top" HorizontalAlignment="Left">
                                        <!-- 画布-->
                                        <s:ProjectDesignerCanvas Background="White" Width="{Binding Canvas.V_width}" Height="{Binding Canvas.V_height}" >
                                            <ItemsControl ItemsSource="{Binding Components}" Style="{StaticResource ComponentItemsControlStyle}" ItemTemplateSelector="{StaticResource SelectionOrComponent}">
                                            </ItemsControl>
                                        </s:ProjectDesignerCanvas>
                                    </ScrollViewer>
                                </Grid>
                            </DataTemplate>
                        </TabControl.ContentTemplate>
                    </TabControl>
                </DockPanel>
            </Border>
        </Grid>
    </DockPanel>

</Window>
